{
    "commands": [
        {
            "command": "nmap -sP <target-ip>",
            "description": "Ping sweep of a target IP address."
        },
        {
            "command": "nmap -sV <target-ip>",
            "description": "Service version detection of the target IP address."
        },
        {
            "command": "nmap -O <target-ip>",
            "description": "OS detection of the target IP."
        }
    ]
}
